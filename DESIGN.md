# flygo 设计方案：自主性边界 · 分级练习 · 奖励评分体系

> 状态：设计稿 v1（2026-09-15）。本文是实施前的方案约定，不是功能说明。
> 原则：**先感知、再决策、后运动；多巴胺不是第一手段，是 RL 阶段的反馈信号。**

---

## 1. 自主性审计（哪些是果蝇自己的神经决定，哪些是代码）

| 行为 / 功能 | 现状 | 驱动来源 | 目标（哪一级起完全自主） |
|---|---|---|---|
| 下在哪里（落子选择） | **神经决定**：64 桶 MBON 读出发放率 argmax | 果蝇脑 | 已自主（L3 起改 RPE 学习） |
| 看哪里 / 头部朝向 | 神经（左右半球不对称→转头）+ 看最后一手 | 果蝇脑 | 已自主；L0 后凝视由视觉输入驱动 |
| 飞行路径（走向目标） | 混合：神经偏航 + 代码归航保底（homingRamp，保证必到） | 混合 | L5 去保底，纯神经导航 |
| 起飞 / 抓取 / 放下时机 | 混合：腿 MN 爆发（真实运动神经元池）触发 + 2.4s 反射兜底 | 混合 | L5 去兜底 |
| 闲时探索飞行 | 触发是代码概率（动机门控），路径是纯神经（偏航/升降/推力） | 混合 | 保留：触发是"动机"，属环境机会 |
| 理毛 / 翅振 / 自理 | 神经（腿/翅 MN 池爆发） | 果蝇脑 | 已自主 |
| 玻璃箱边界与越界惩罚 | 代码约束（环境规则=墙壁）+ 多巴胺 −0.4 | 环境 | 保留（合理：物理世界规则） |
| 棋子出现 / 棋盘状态 | 代码（环境） | 环境 | 保留 |
| 重开一局 / 暂停 / 单步 / 存档 / 档位 / 速度 | **实验员控制**（代码、手动） | 实验员 | 保留手动（这是实验控制台，不该由果蝇决定） |
| 手动奖励/惩罚按钮 | 实验员控制（调试用） | 实验员 | 保留，但只在练习模式里作为干预手段 |
| 视觉输入（它"看到"什么） | **工程替代**：eval_cell 显著度→输入群（已披露，不是视觉） | 代码 | L0 起替换为拓扑视觉编码 |

**结论**：决策与自理动作已经是神经驱动；"混合"部分都有代码保底（工程诚信已在页面披露）；真正缺的是**感觉输入的真实性**——这正是分级练习从 L0 做起的原因。

---

## 2. 输入 / 输出通路（练习的物理基础）

### 输入（感觉端）
- **L0-L1 用拓扑视觉编码**（不先上完整视网膜）：15×15 棋盘 → 每个格子映射到一小群真实视觉神经元（MaleCNS vis 池按解剖位置分组，1536 输入群已是其子集）。刺激 = 该群电流注入。
  - 数据基础已具备：`flygo_data.npz` 内 `in_groups`（64 桶×24 神经元）+ 注释表 superclass（ol_sensory/visual_projection ~15k 神经元）。
  - 之后升级（可选）：flygym Retina（721 小眼）做真实成像，把棋盘渲染成果蝇视网膜图。
- **propriceptive/接触输入（已有）**：接近度 → 腿 MN 电流（motor_step），玻璃箱碰撞 → 惩罚。

### 输出（动作端）
- **决策读出（已有）**：64 桶 MBON 群发放率 argmax（真实 MBON 池，KC→MBON 可塑通路就是决策通路）。
- **运动读出（已有）**：腿 MN（抓握/理前肢）、翅 MN（翅振）、颈 MN（转头）、hind MN（理后肢）、全脑半球不对称（偏航/升降/推力）。
- **选择置信度（L2 新增）**：两个候选读出的发放率比值 = 它的"把握程度"，用于 RPE 计算。

### 学习基质（已有，保留）
KC→MBON 三因子可塑（61,210 条真实突触，多巴胺门控，乘法增益 m∈[-0.9,4]），持久化双档位（trained/pristine）+ 新增 **training 档**（练习进度独立存档，不污染对弈档）。

---

## 3. 分级练习课程（L0-L5）

> 每级：任务 → 输入/输出 → 学习规则（关键：多巴胺何时才允许出现）→ 通过标准 → 实验员控件。
> 每级成绩独立存 `saves/levels/`；界面新增"练习模式"选择器 + 单步练习按钮 + 该级成绩单。

### L0 感知校准（无奖励）
- 任务：棋盘随机亮一个格子（刺激），测量果蝇脑对"位置"的表征质量。
- 输出：读出群对刺激位置的解码准确率（不需要它做任何动作）。
- 学习规则：**无多巴胺、无惩罚**。可选 Hebbian 共激活（输入-读出），默认纯测量。
- 通过标准：解码准确率 ≥90%（若编码本身够好，此级几乎免学——它是基线体检）。

### L1 定位练习（微小初级强化，非对弈多巴胺）
- 任务：亮一格 → 果蝇"指"出位置（64 桶 argmax），单步进行。
- 反馈：指对 → 小奖励 r=+0.3（相当于找到食物的甜味）；指错 → **无惩罚**，只记录。
- 通过标准：连续 50 试次正确率 ≥80%。
- 实验员控件：单步、换刺激、查看正确率曲线。

### L2 二选一判别（多巴胺正式登场，改为 RPE）
- 任务：给两个候选格（棋理上一个明显更优，judge 判 Q 差 ≥0.3 才计入），果蝇二选一。
- 反馈：**r = 实际对错 − 预期**（预期 = 两个读出的置信度比例）。这才是多巴胺的生物学含义（奖赏预测误差），而不是"直接打分"。
- 通过标准：滑动 100 试次正确率 ≥75%。

### L3 全盘决策（现有系统升级）
- 任务：64 候选正常落子（现在的 arena）。
- 升级：奖励从 Q→r 改为 **RPE + 资格迹**（连续几手的贡献按迹衰减分账，解决"这一手好是因为上一手铺垫"的信用分配）。
- 通过标准：对随机落子基线胜率 ≥70%。

### L4 对弈与终局信用（现有 arena 升级）
- 升级：终局 ±1 不再只打最后一手，按 TD(λ) 回传到整局资格迹。
- 评分：胜率、平均 Q、对手强度分级（先自我对弈，后对固定基线）。

### L5 具身闭环（终极目标）
- 把 L0-L1 的视觉接入现在的飞行/抓取：果蝇先飞向它**自己看到**的目标区，落点保底（homingRamp）与抓取反射兜底撤除，只保留玻璃箱碰撞。
- 评分：完整成功率（看到→飞到→放下）与耗时。

### 顺序与回退
- 任一级不达标不进入下一级；L2+ 的多巴胺设置只在 ≥L2 生效（UI 锁定）。
- 练习档（training profile）与对弈档（trained）互不影响；"零训练"档照旧。

---

## 4. 奖励 / 评分体系（统一约定）

| 层 | 信号 | 允许出现的级别 | 说明 |
|---|---|---|---|
| 感知层 | 无奖励（测量/Hebbian） | L0 | 体检，不打分 |
| 初级强化 | 固定小 r（食物级） | L1 | 不是对弈输赢，幅度 ±0.3 封顶 |
| 奖赏预测误差 RPE | r − 预期(置信度) | L2-L4 | 多巴胺的生物学含义 |
| 终局回报 | TD(λ) 回传 | L4 | 资格迹分账 |
| 环境惩罚 | 撞墙 −0.4（限频） | 全部 | 物理世界规则 |

**打分显示**（新增"成绩单"面板）：每级正确率/平均 Q/学习曲线/试次数；综合评级 = 感知(L0-1) + 决策(L2-3) + 对弈(L4) + 具身(L5) 分项显示。外部 judge 全程身份 = "教练/裁判"（页面已披露：它不是果蝇内部功能）。

---

## 5. 界面功能组件清单

**保持手动（实验员控制台，合理）**：暂停/继续、单步下一手、重开一局、保存/加载/清档、档位切换、速度、本能加权 α、手动多巴胺按钮（调试）。
**新增（练习体系）**：级别选择 L0-L5、单步练习、该级成绩单、（L5 前）"显示它看到的刺激"可视化、多巴胺 UI 锁（L0-L1 灰掉）。
**果蝇自主（神经驱动，不设按钮）**：落子选择、凝视/转头、飞行路径（L5 后无保底）、抓放时机（L5 后无反射兜底）、理毛/翅振自理。

---

## 6. 实施顺序（建议）

1. **M1 练习框架**：training 档 + 级别选择 + L0/L1（拓扑视觉编码 + 定位任务 + 成绩单）。这一步就让"感知→位置"跑通，且不动对弈系统。
2. **M2 RPE 化**：L2 二选一 + 置信度读出 + RPE；L3 换 RPE+资格迹。
3. **M3 终局信用**：L4 TD(λ)。
4. **M4 具身闭环**：L5 去保底。
5. **M5（可选）**：flygym Retina 真实视觉成像替换拓扑编码。

---

## 7. 已知边界（诚实披露）

- 判分来自外部 judge（经典棋型评估器）——在 L2 之前它是"题目出题人"，L2 之后是"教练"，不是果蝇内部能力。
- 拓扑视觉编码是简化的视觉（每格一小群输入神经元），不是视网膜成像；升级路径见 M5。
- 飞行/抓取目前有工程保底（到点保证、2.4s 反射），L5 才撤除；此前不宣称"完全具身自主"。
